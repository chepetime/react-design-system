import { Button, Stack } from "@chepe/ui";

# Button

Introduction for the Button Compponent

```tsx filename="app.tsx" {4} copy
import { Button } from "@chepe/ui";

export default function App() {
  return <Button>Hello world</Button>;
}
```

<br />

<Button>Hello world</Button>

<br />

## Variants

import { useState } from "react";

{/* Import CSS modules */}

export const ButtonTypes = () => {

return (

<div style={{ paddingTop: "16px" }}>
  <Stack spacing={16} divider={false} horizontal={false}>
    <Button>Normal</Button>
    <Button variant="ghost">Ghost</Button>
    <Button variant="text">Text</Button>
    <Button variant="link">Link</Button>
  </Stack>
</div>
); };

<ButtonTypes />

## Component

{/* Import CSS modules */}
import styles from '../../components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>
        Clicked {count} times
      </button>
    </div>
  );
};

<Counter />

## External Component

import Counters from "../../components/counters";

<Counters />
