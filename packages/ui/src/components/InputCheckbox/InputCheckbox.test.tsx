import React from "react";
import { render } from "@testing-library/react";
import { InputCheckbox } from "./InputCheckbox";

describe("InputCheckbox", () => {
  test("renders the InputCheckbox component", () => {
    render(<InputCheckbox />);
  });
});
