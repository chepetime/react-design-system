import React from "react";
import { render } from "@testing-library/react";
import { InputSearch } from "./InputSearch";

describe("InputSearch", () => {
  test("renders the InputSearch component", () => {
    render(<InputSearch />);
  });
});
