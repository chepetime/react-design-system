import React from "react";
import { render } from "@testing-library/react";
import { InputRadioButton } from "./InputRadioButton";

describe("InputRadioButton", () => {
  test("renders the InputRadioButton component", () => {
    render(<InputRadioButton />);
  });
});
