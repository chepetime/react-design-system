{
  "name": "token-manager",
  "version": "1.0.0",
  "description": "Boilerplate for Node apps with Typescript, Jest and Module aliases",
  "license": "MIT",
  "author": "José Manuel Gulias Lugo",
  "main": "build/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "clean": "rm -rf .turbo && rimraf ./build && rm -rf node_modules",
    "format": "eslint src/**/*.ts --fix",
    "lint": "eslint src/**/*.ts",
    "start": "nodemon --quiet",
    "test": "jest",
    "test:live": "jest --watch"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^18.6.5",
    "@typescript-eslint/eslint-plugin": "^5.33.0",
    "@typescript-eslint/parser": "^5.33.0",
    "eslint": "^8.21.0",
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.0",
    "typescript": "^4.7.4"
  }
}
