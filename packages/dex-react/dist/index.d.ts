import * as React from 'react';

interface DexButtonProps {
    children?: React.ReactNode;
}
declare function DexButton(props: DexButtonProps): JSX.Element;
declare namespace DexButton {
    var displayName: string;
}

export { DexButton, DexButtonProps };
