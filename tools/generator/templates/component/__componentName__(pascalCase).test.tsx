import React from "react";import { render } from "@testing-library/react";
import { __componentName__(pascalCase) } from "./__componentName__(pascalCase)";

describe("__componentName__(pascalCase)", () => {
  test("renders the __componentName__(pascalCase) component", () => {
    render(<__componentName__(pascalCase) />);
  });
});
