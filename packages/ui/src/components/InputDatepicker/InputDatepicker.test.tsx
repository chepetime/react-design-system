import React from "react";
import { render } from "@testing-library/react";
import { InputDatepicker } from "./InputDatepicker";

describe("InputDatepicker", () => {
  test("renders the InputDatepicker component", () => {
    render(<InputDatepicker />);
  });
});
