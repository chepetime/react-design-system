import React from "react";
import { render } from "@testing-library/react";
import { InputPassword } from "./InputPassword";

describe("InputPassword", () => {
  test("renders the InputPassword component", () => {
    render(<InputPassword />);
  });
});
